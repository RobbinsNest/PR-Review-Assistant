# REFLECTION — 反思报告

> ⚠️ **学术规范声明（必读）**：按《AI4SE 期末项目通用要求》§六，本反思报告**必须由学生本人撰写**，禁止使用 AI 代写。以下仅提供**问题框架与写作提示**（AI 辅助润色可用，但需标注）。请逐条用自己的话回答，替代本模板占位内容后提交。

## 一、必须回答的问题（建议逐条展开，总字数 1500–2500）

1. **哪些 Superpowers 技能发挥了最大作用，哪些"形式大于实质"？**
   - 提示：brainstorming 的逐节确认是否真的帮你收敛了需求？TDD 红绿循环、两阶段评审、最终评审各自的价值？有没有哪一步只是走了流程而没产生实际价值？

2. **TDD 强制在 AI 协作下是阻碍还是放大器？**
   - 提示：subagent 是否因为"先写失败测试"而更少跑偏？测试先行是否拖慢了速度？哪些测试最值得写、哪些是仪式？

3. **subagent-driven 工作流让智能体能自主运行多久而不偏离主题？**
   - 提示：本项目中单个 subagent 的任务颗粒度（约 2-5 分钟 step / 一次会话一个 task）；最长自主推进的节点；在哪里需要你介入纠偏（可引用 AGENT_LOG 中的人工干预条目）。

4. **什么样的 task 颗粒度最优？**
   - 提示：T1-T5 与 T6-T9 的粒度差异；是否出现过"太大导致跑偏"或"太小导致开销过大"？

5. **SPEC / PLAN 质量如何影响实现质量？（举一个"规约不清导致 subagent 偏离"的具体案例）**
   - 提示：冷启动验证暴露的缺陷（ERROR_HTTP 表位置、红态描述）；T4 语言策略歧义；T13 total 语义；T16 两个后端契约缺口。

6. **你最有效的 prompt / context 策略是什么、为什么有效？**
   - 提示：给 subagent 的 brief 只含 task 文本 + 全局约束 + 环境事实；评审模板固定输出格式；"fold-in"（把上轮评审的 minor 折入下一任务指令）等。

7. **凭据与分发这两条工程要求，迫使你想清楚了哪些原本会忽略的问题？**
   - 提示：keyring vs .env 的威胁模型；base_url 校验防 key 外泄；会话级 GitHub token；Docker 多阶段构建；CI 双配置（GitHub Actions + .gitlab-ci.yml 的 unit-test job）。

8. **如果重做你会改变什么？**
   - 提示：环境（网络/代理）准备顺序；worktree 并行时对共享文件（main.py）的合并策略；前端与后端的契约先行（先定 API schema 再并行实现）；示例 PR 的钉选时机。

9. **你对 Superpowers 这套方法论的批判——它假设了什么，这些假设在你的项目里成立吗？**
   - 提示：假设"评审能抓出真实缺陷"（本项目 8+ 例，成立）；假设"subagent 会如实自评"（部分成立）；假设"TDD 成本可接受"；它对人类判断力的依赖（"做什么"与"做对了吗"仍由人负责）等。

## 二、写作建议
- 用具体事实支撑观点：引用 commit hash、task 编号、评审发现、AGENT_LOG 条目，比泛泛而谈更有说服力。
- 不要只夸流程，也要指出真实的摩擦点（网络受限、并行 worktree 冲突、评审反复等）。
- 字数控制：约 2000 字；可在句首标注"以下内容由我本人撰写"。
