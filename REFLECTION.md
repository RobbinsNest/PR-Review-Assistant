# 反思报告（经AI辅助润色）

这个项目从零开始走完了 Superpowers 的完整流程：brainstorming 把想法收敛成规约，writing-plans 把规约拆成任务，冷启动验证暴露规约的漏洞，然后五个 worktree 并行开发、每个任务两阶段评审、最后逐个合并并补齐分发。整个过程比我想象的长得多，中间踩了不少坑，也有很多现在回头看会改掉的做法。

## 一、流程里真正有价值的部分

对我帮助最大的是两阶段评审和 fix round。subagent 写代码时确实很专注，但"它觉得自己写完了"和"代码真的符合要求"是两回事。T4 的函数边界提取就是个例子：评审一轮抓出 Go/Rust 的 func/fn 根本没被识别，一轮抓出 Python 多行签名会让函数窗口丢失，修完之后又发现单行套件会把下一个函数吞进窗口，前后修了三轮才稳定。如果不是每次都先做 spec 合规检查再做代码质量检查，这些错误会一路带进最终结果。反过来说，writing-plans 对我的价值更多是逼我把每个模块的接口想清楚，计划里写的那些测试代码倒没多大用——实际写的时候基本都改过了，真正有效的还是每个任务的 Produces 和 Consumes 接口定义。

TDD 在 AI 协作下给我的感觉是放大器而不是阻碍。subagent 拿到"先写失败测试"的指令后很少跑偏，因为红态给了它一个明确的停止条件；我自己改代码时，有测试兜底也大胆很多。但 TDD 不是免费的：前端有几个组件，测试要先 mock 一堆东西才能跑起来，成本和收益明显不成比例，比如进度页计时器清理那个测试。我的结论是核心逻辑和跨模块接口必须 TDD，纯展示性的 UI 细节可以适当放宽。

## 二、subagent 能自主跑多久，任务拆多大合适

单个任务只要颗粒度控制得好，subagent 基本能一口气完成，不用中途打断。最长的一段应该是 T9 任务管理器和 SSE，那个任务本身就偏大，subagent 还自己顺手修了两个自评发现的问题——cancel 发生在任务启动前的状态残留，以及错误提示包装时裸 raise 丢掉了新错误信息。真正需要我介入的地方几乎都出现在任务边界：比如两个并行 worktree 同时改 main.py，以及前端在等一个后端根本不存在的字段。

这次一共 17 个任务，跨度差别很明显。T1 脚手架、T2 数据模型这种半天能完的任务很顺；T9 这种要同时设计状态机、SSE 和 API 的就偏大，评审时也确实挖出了更多问题，如果重做我会把它再拆开。反过来，像 T3 GitHub fetcher 那种拆得太碎又会让评审轮次翻倍。总的标准还是那句：一个 subagent 在一个会话里能完成，而且评审能独立判断对错。

## 三、规约不清的代价

最典型的是 ERROR_HTTP 错误码映射表。计划里 T1 要求实现这张表，但具体的映射关系写在 T12 里，T1 的 subagent 只能自己猜，碰巧猜对了。冷启动验证把这个问题暴露得很明显——那个全新 agent 没有我们对话里的任何上下文，遇到这种跨任务引用就直接停下来问。另一个例子是 T13 的 total 字段：计划写"返回 items 和 total"，前端按真实总数实现分页，后端一开始返回的却是页面大小，评审抓出来才改对。这些教训说明，跨任务引用的常量和接口字段的语义必须在计划里就近写死，不能指望 subagent 自带上下文。

## 四、几条最有效的经验

给 subagent 的 brief 我后来固定只装三样东西：任务文本、全局约束、环境事实（比如 venv 路径、pytest 临时目录怎么处理），不塞对话历史。看起来简单，效果却很好——subagent 不会被无关上下文带偏，也不会因为缺环境信息卡住。另一个有效做法是把上一轮评审的 minor 项折进下一个相关任务的指令里，比如 T6 评审说 hunk 范围会过近似变更行，T7 的指令里就明确要求校验阶段做真实变更行判定，小问题不用专门开 fix round，跟着主流程就消掉了。

凭据和分发这两条要求直接改变了设计。最初我只打算把 LLM key 存在 .env 里，但"必须安全存储、绝不进 git、绝不写日志"逼着我认真比较了 keyring 和 .env，最后做成 keyring 优先、.env 兜底、所有接口只回掩码。更意外的是评审阶段挖出的一个漏洞：设置页允许改 base_url，而连通性测试会把 key 发给这个地址，一旦部署到公网，攻击者改一个自己控制的 https 地址就能把 key 钓走，于是又加了 base_url 校验，只允许公网 https、拒绝私网和单标签主机。GitHub token 则只在会话内存里存活，任务结束就丢。这些如果不做分发这条线，根本不会想到。Docker 多阶段构建也是被这条要求推着做的，顺带把 GitHub Actions 和 GitLab 的 unit-test job 都补齐了。

## 五、如果重做会改什么

第一，先解决网络问题再开工。项目期间本机连不上 github.com，折腾很久才发现要走本地代理，这浪费了不少时间。第二，前端和后端应该先把 API 契约定死再并行开发。T16 把 diff 高亮和历史页都做好了，结果发现后端结果里根本没有 diff 数据，也没人往历史表写记录，等于白做了一半，还要回头补两个集成 seam。第三，示例 PR 这种"谁提供数据"的问题应该在计划里写明归属，而不是让前端硬编码一个 URL。第四，并行 worktree 改同一个文件时，应该一开始就约定好合并策略，而不是等 PR 冲突了再人工调和。

## 六、对这套方法本身的看法

Superpowers 最核心的假设是评审能抓到 subagent 的真实缺陷，这个假设在我的项目里基本成立——十五六个问题都是评审抓出来的，包括并发竞态和凭据泄露这种严重的。但它还假设 subagent 会如实自评，这部分只对了一半：subagent 报告的测试结果基本可信，可它对"需求理解得对不对"的自评经常不准，需要评审来兜底。另一个隐含假设是 TDD 的成本可以接受，实际在 UI 场景下成本偏高。最重要的感受是：流程可以做得很重，但"做什么"和"做对了吗"最终还是人说了算。冷启动验证和两阶段评审的价值恰恰在于把人的判断放在关键节点上，而不是让流程替人思考。如果只是把题目丢给流程，代码能跑，但设计上的欠账——比如跨模块契约——会一直留到最后，直到某个真实用户点一下按钮才暴露。
